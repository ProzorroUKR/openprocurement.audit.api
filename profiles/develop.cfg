[buildout]
extends = sandbox.cfg
extensions = mr.developer
auto-checkout = *
always-checkout = force

[sources]
openprocurement.audit.inspection = ${remotes:gh}openprocurement.audit.inspection branch=dev