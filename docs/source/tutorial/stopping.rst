Stop Monitoring
===============

As soon as the monitoring in ``active``, ``addressed``, ``declined`` status, SAS-employee can terminate monitoring with ``stopped`` status with providing the ``description`` in ``stopping`` report

.. include:: http/monitoring-to-stopped.http
   :code:
