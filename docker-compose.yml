version: '3'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - couchdb
    ports:
    - "80:80"
  couchdb:
    image: "couchdb:1.6"
    environment:
      COUCHDB_USER: op
      COUCHDB_PASSWORD: op
